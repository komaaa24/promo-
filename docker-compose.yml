services:
  postgres:
    image: postgres:16-alpine
    container_name: promo_bot_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: promo_bot
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
